# ToyWake 后端环境变量示例
# 复制为 .env 后按需修改。默认 mock 模式无需任何 AI 配置即可离线运行。

# AI 模式：mock（离线固定/模拟）| ai（调用真实模型）
AI_MODE=mock

# OpenAI-compatible 配置（仅 AI_MODE=ai 时需要）
AI_BASE_URL=
AI_API_KEY=
AI_MODEL=
AI_TIMEOUT_SECONDS=10

# 数据库
DATABASE_URL=sqlite:///./toywake.db

# 每个游戏会话最多生成的火花数
MAX_SPARKS_PER_SESSION=5
